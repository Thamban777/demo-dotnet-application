name: Build and Analyze .NET Project
on:
  push:
    branches: [ "main" ]
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build:
    name: Build and Analyze
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Install SonarScanner
        run: dotnet tool install --global dotnet-sonarscanner

      - name: Install dotnet-coverage
        run: dotnet tool install --global dotnet-coverage

      - name: Begin Sonar Analysis
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          dotnet-sonarscanner begin \
            /k:"todo-app" \
            /n:"todo-app" \
            /d:sonar.host.url="${{ secrets.SONAR_HOST_URL }}" \
            /d:sonar.token="${{ secrets.SONAR_TOKEN }}" \
            /d:sonar.qualitygate.wait=true \
            /d:sonar.cs.vscoveragexml.reportsPaths=coverage.xml

      - name: Restore Dependencies
        run: dotnet restore TodoApi/TodoApi.csproj

      - name: Build Main Project
        run: dotnet build TodoApi/TodoApi.csproj --no-incremental --configuration Release --no-restore

      - name: Build Test Project
        run: dotnet build TodoApi.Tests/TodoApi.Tests.csproj --no-incremental --configuration Release --no-restore

      - name: Run Tests with Coverage
        run: |
          dotnet-coverage collect \
            "dotnet test TodoApi.Tests/TodoApi.Tests.csproj --no-build --configuration Release" \
            -f xml \
            -o coverage.xml

      - name: End Sonar Analysis
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          dotnet-sonarscanner end \
            /d:sonar.token="${{ secrets.SONAR_TOKEN }}"